apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "race-condition.fullname" . }}-test-connection"
  labels:
    {{- include "race-condition.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "race-condition.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
