apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "private-api.fullname" . }}-test-connection"
  labels:
    {{- include "private-api.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "private-api.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
