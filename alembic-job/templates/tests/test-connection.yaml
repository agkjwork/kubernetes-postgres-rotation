apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "alembic-job.fullname" . }}-test-connection"
  labels:
    {{- include "alembic-job.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "alembic-job.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
